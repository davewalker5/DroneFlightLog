@model DroneFlightLog.Mvc.Models.EditLocationViewModel

@{
    ViewData["Title"] = "Edit Location";
}

<div class="container-fluid">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="row">
            <div class="col-md">
                <span style="font-size: 1.2rem"><strong>Edit Location</strong></span>
            </div>
        </div>
        <hr />

        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="row">
                <div class="col-md">
                    <span>@Html.Raw(Model.Message)</span>
                </div>
            </div>
            <br />
        }

        @await Html.PartialAsync("Location", Model);
    }
</div>
